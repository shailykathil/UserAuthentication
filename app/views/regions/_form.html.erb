<%= form_for(region) do |f| %>
  <% if region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
      <% region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :region_name, :class=>"col-sm-2 control-label"%>
      <div class="col-sm-4  inputGroupContainer">
    <%= f.text_field :region_name, :placeholder => "Region",:class=>"form-control" %>
  </div>
</div>
<br><br><br>
  <div class="actions">
  <div class="col-sm-2  inputGroupContainer">
    <%= f.submit :class=>"btn btn-success" %>
    </div>
  </div>
<% end %>
