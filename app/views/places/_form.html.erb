<%= form_for(place) do |f| %>
  <% if place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(place.errors.count, "error") %> prohibited this place from being saved:</h2>

      <ul>
      <% place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="inline">
      <div class="form-group">
      <%= f.label :place_name ,:class=>"col-sm-2 control-label"%>
        <div class="col-sm-4  inputGroupContainer">
       <%= f.text_field :place_name ,:class=>"form-control",:placeholder => "Place Name"%>
      </div>
    </div>
    <div class="form-group">
       <%= f.label :node_name ,:class=>"col-sm-2 control-label"%>
         <div class="col-sm-4  inputGroupContainer">
          <%= f.text_field :node_name ,:class=>"form-control",:placeholder => "Node Name"%>
        </div>
  </div>
</div>
<br><br>
  <div class="form-group">
        <label class="col-sm-2 control-label">Company</label>
        <div class="col-sm-7  inputGroupContainer">
         <%= f.select(:company_id, Company.all.collect {|u| [u.name, u.id]},{},:prompt => 'Select Company',:class=>"form-control") %>

     </div>
  </div>
<br><br><br><br>
  <div class="actions">
     <div class="col-sm-2  inputGroupContainer">
    <%= f.submit :class=>"btn btn-success"%>
  </div>
  </div>
<% end %>
