<%= form_for(state) do |f| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
    <%= f.label :state_name, :class=>"col-sm-2 control-label" %>
    <div class="col-sm-4  inputGroupContainer">
    <%= f.text_field :state_name, :placeholder => "State Name",:class=>"form-control" %>
  </div>
  </div>
  <br><br>
   <div class="form-group">
    <%= f.label :country_id , :class=>"col-sm-2 control-label"%>
    <div class="col-sm-4  inputGroupContainer">
    <%= f.select(:country_id, Country.all.collect {|u| [u.country_name, u.id]},{},:prompt => 'Select Country',:class=>"form-control") %>
  </div>
  </div>
  <br><br>
  <div class="actions">
    <div class="col-sm-2  inputGroupContainer">
    <%= f.submit :class=>"btn btn-success" %>
    </div>
    </div>
<% end %>
