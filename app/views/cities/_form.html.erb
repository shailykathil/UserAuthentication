<%= form_for(city) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :city_name , :class=>"col-sm-2 control-label"%>
     <div class="col-sm-4  inputGroupContainer">
    <%= f.text_field :city_name , :placeholder => "City Name",:class=>"form-control"%>
  </div>
  </div>
  <br><br>
  <div class="form-group">
    <%= f.label :state_id , :class=>"col-sm-2 control-label"%>
     <div class="col-sm-4  inputGroupContainer">
    <%= f.select(:state_id, State.all.collect {|u| [u.state_name, u.id]},{},:prompt => 'Select State',:class=>"form-control") %>
  </div>
  </div>
  <br><br>
  <div class="actions">
   <div class="col-sm-2  inputGroupContainer">
    <%= f.submit :class=>"btn btn-success" %>
    </div>
  </div>
<% end %>
